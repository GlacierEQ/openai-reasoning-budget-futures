# Frontier decision — 2026-08-15 HST

## Primary-source signal

OpenAI's GPT-5.6 preview documents two execution changes directly relevant to this specialist:

- a new `max` reasoning-effort level;
- an `ultra` mode that uses subagents to accelerate complex work.

OpenAI also documents explicit cache breakpoints and a 30-minute minimum cache life for GPT-5.6, and its API changelog lists an August 13 Ultrafast-mode announcement.

Sources:
- https://openai.com/index/previewing-gpt-5-6-sol/
- https://developers.openai.com/api/docs/changelog

## Classification

**EXPERIMENT**

The signal strengthens the case for multidimensional precommitment, but it does not justify encoding provider-internal scheduling, undocumented subagent counts, or speculative cost multipliers.

## Architecture-lane impact

Existing unique value is preserved: `ReasoningBudgetLedger.mint(future_id, max_tokens)` and `spend(future_id, tokens, entropy)` remain valid.

The experiment adds explicit optional limits for:

1. token budget;
2. cumulative tool calls;
3. per-execution parallel-agent count.

`authorize_execution` refuses before mutation when any declared envelope is exceeded. Entropy freeze remains intact.

## Tower decision

Retain Python for the deterministic ledger/reference mechanism in this repository. This change is state-machine and invariant heavy, not a browser/UI, distributed-coordination, accelerator, or realtime boundary. No new runtime is justified by the present mechanism. The existing JavaScript lane remains untouched.

## Prior-gain preservation

- token-only mint/spend API preserved;
- entropy circuit-break preserved;
- deterministic receipt fingerprint preserved and extended;
- no provider affiliation or proprietary-access claim introduced;
- no provider-internal behavior is asserted beyond cited public facts.

## Reversible proof target

The branch must prove:

- legacy token spending still passes;
- parallelism overrun refuses without token/tool mutation;
- cumulative tool-call overrun refuses without second-spend mutation;
- invalid envelope configuration fails at mint;
- entropy freeze still blocks later spend;
- existing adversarial and position-contract tests remain green.

## Promotion rule

ADMIT only if repository-native CI passes on the exact branch head and the diff contains no regression of existing mechanisms. Otherwise retain as EXPERIMENT and repair or revert.
